const API_BASE_URL = "http://localhost:8080";

const API_ENDPOINTS = {
  LOGIN: "/login",
  VALIDATE_TOKEN: "/validateToken",
  TODO: "/todo",
  SEARCH: "/todo/search",
};

export { API_BASE_URL, API_ENDPOINTS };
